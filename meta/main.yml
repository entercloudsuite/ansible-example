---
galaxy_info:
  author: Federico Minzoni
  description: Example playbook to use as an implementation reference.
  company: Enter srl
  license: MIT
  min_ansible_version: 2.2
  platforms:
    - name: Ubuntu
      versions:
        - xenial
  galaxy_tags:
    - molecule
    - example
    - cowsay

dependencies:
  - ansible-role-nginx
  # List your role dependencies here, one per line.
  # Be sure to remove the '[]' above if you add dependencies
  # to this list.
